# 扣单宝本地可落地版：项目技术证明

生成时间：2026-08-02  
项目目录：`C:\Users\xquan\Desktop\扣数宝\portable\print`  
项目版本：`1.1.2`

> 本文档用于展示当前项目已经具备“单独完整项目落地”的技术条件：源码可维护、前后端可构建、后端可独立运行、Electron 可自动拉起后端、测试可复现、Windows 安装包/便携版已生成。

## 1. 项目定位

`扣单宝本地可落地版` 是一个本地桌面应用项目，包含：

- 前端：React + TypeScript + Vite 构建的业务界面；
- 后端：Python 本地 HTTP/WebSocket 服务，提供扣数、订单、平台协议等本地 API；
- 桌面端：Electron 外壳，负责窗口、授权回调、本地后端进程管理；
- 打包：PyInstaller 打包 Python 后端，electron-builder 打包 Windows 安装包和便携版；
- 测试：统一 `npm test` 入口覆盖前端类型检查、后端协议、扣数引擎、弹幕调度、平台集成和 runtime helper。

## 2. 技术架构

```text
Windows 桌面用户
   │
   ▼
Electron 桌面壳：electron/main.js
   │  1. 启动窗口
   │  2. 自动启动本地后端
   │  3. 读取后端 ready JSON
   │  4. loadURL 到真实动态端口
   ▼
Python 本地后端：backend/server.py
   │  HTTP API / WebSocket / 本地数据目录
   ▼
前端页面：frontend-src/dist
   │
   ▼
本地数据：开发模式 backend/global-store.json；打包模式 %APPDATA%\Koudanbao
```

### 核心运行链路

1. 用户启动 `扣单宝.exe`；
2. Electron 判断是否存在 `KOUDANBAO_URL`；
3. 如果没有外部后端地址，Electron 自动启动内置后端：`resources/backend/koudanbao-backend.exe`；
4. 后端绑定动态端口并输出：`{"event":"ready","host":"127.0.0.1","port":...}`；
5. Electron 读取实际端口并加载本地页面；
6. 业务数据默认写入用户目录，避免安装目录写入失败。

## 3. 技术栈清单

| 模块 | 技术 | 证据文件 |
|---|---|---|
| 前端框架 | React `19.2.8` | `frontend-src/package.json` |
| 前端构建 | Vite `8.2.0` | `frontend-src/package.json` |
| 类型系统 | TypeScript `7.0.2`，支持 `tsc --noEmit` | `frontend-src/package.json` |
| UI/组件 | Radix UI、lucide-react、Tailwind CSS | `frontend-src/package.json` |
| HTTP 客户端 | axios | `frontend-src/package.json` |
| 后端服务 | Python 单体本地服务 | `backend/server.py` |
| 后端打包 | Rust (axum) release | `scripts/build-backend.mjs`（跨平台） |
| 桌面端 | Electron `43.2.0` | `electron/package.json` |
| 桌面打包 | electron-builder `26.0.0` | `electron/package.json` |
| 自动化测试 | Node.js + Python + PowerShell 脚本 | `tests/`、`scripts/test-backend-protocol.ps1` |

## 4. 独立落地能力证明

### 4.1 根目录统一入口

根目录 `package.json` 已提供完整生命周期命令：

```powershell
npm test
npm run build
npm run build:backend
npm run package:dir
npm run package
```

说明：不需要分别手动进入前端、后端、Electron 子目录拼命找命令，根目录即可完成测试、构建和打包。

### 4.2 前端可维护、可构建

- 前端源码目录：`frontend-src/`
- 类型检查命令：`npm --prefix frontend-src run typecheck`
- 构建命令：`npm --prefix frontend-src run build`
- 构建产物：`frontend-src/dist/`

### 4.3 后端可独立运行、可打包

后端支持三种数据目录模式：

| 场景 | 数据目录 |
|---|---|
| 开发模式默认 | `backend/global-store.json` |
| 指定环境变量 | `KDB_DATA_DIR=<目录>` |
| 打包后默认 | `%APPDATA%\Koudanbao` |

后端支持动态端口和 ready JSON：

```powershell
python backend/server.py --host 127.0.0.1 --port 0 --print-json-ready
```

这解决了端口冲突问题，也使 Electron 能自动识别实际服务地址。

### 4.4 桌面端可自动拉起后端

Electron 主进程已实现：

- 开发模式：自动启动 `python backend/server.py`；
- 打包模式：自动启动 `resources/backend/koudanbao-backend.exe`；
- 读取后端 ready JSON；
- 将页面加载到真实后端地址；
- 程序退出时清理后端子进程；
- 支持 `KOUDANBAO_URL` 外部后端覆盖，便于调试和联调。

证据文件：`electron/main.js`。

### 4.5 Windows 安装包和便携版已生成

| 产物 | 大小 | 生成时间 |
|---|---:|---|
| `backend-dist/koudanbao-backend.exe` | 12.18 MB | 2026-08-02 04:35:28 |
| `dist/扣单宝 Setup 1.1.2.exe` | 111.64 MB | 2026-08-02 04:41:53 |
| `dist/扣单宝 1.1.2.exe` | 111.33 MB | 2026-08-02 04:42:01 |
| `dist/win-unpacked/扣单宝.exe` | 215.25 MB | 2026-08-02 04:41:00 |
| `dist/win-unpacked/resources/backend/koudanbao-backend.exe` | 12.18 MB | 2026-08-02 04:35:28 |

## 5. 当前验证结果

已执行：

```powershell
npm test
```

结果：全部通过。

| 验证项 | 结果 |
|---|---|
| 前端 TypeScript typecheck | 通过 |
| 后端 API 协议测试 | 69 通过，0 失败 |
| 扣数引擎测试 | 38 通过，0 失败 |
| 弹幕调度器测试 | 36 通过，0 失败 |
| 平台集成测试 | 27 通过，0 失败 |
| runtime helpers | 42 pass，0 fail |

测试中出现的提示：

```text
SKIP 原版逐字节比对：未设置 KDB_ORIGINAL_PRELOAD_DIR 或目录不存在
```

这是预期行为，不影响测试结论。未设置原版 preload 目录时，只跳过“与原版逐字节对比”，其他平台集成结构和逻辑仍正常验证。

## 6. 可复现命令

### 安装依赖

```powershell
npm install
npm --prefix frontend-src install
npm --prefix electron install
```

### 本地验证

```powershell
npm test
```

### 构建前端

```powershell
npm run build
```

### 打包后端

```powershell
npm run build:backend
```

### 生成免安装目录版

```powershell
npm run package:dir
```

### 生成安装包和便携版

```powershell
npm run package
```

## 7. 交付形态

当前项目已经可以按以下方式交付：

1. **源码交付**：整个项目目录，包含 `package.json`、`frontend-src/`、`backend/`、`electron/`、`scripts/`、`tests/`；
2. **安装包交付**：`dist/扣单宝 Setup 1.1.2.exe`；
3. **便携版交付**：`dist/扣单宝 1.1.2.exe`；
4. **解压目录版交付**：`dist/win-unpacked/`。

## 8. 结论

从工程结构、构建入口、后端进程管理、数据目录隔离、自动化测试、Windows 打包产物几个维度看，该项目已经具备独立落地条件。

可以直接把以下文件作为最终给用户安装/运行的交付物：

```text
dist/扣单宝 Setup 1.1.2.exe
dist/扣单宝 1.1.2.exe
```

如果需要二次开发或交给其他技术人员维护，则交付完整源码目录，并让对方按本文档中的“可复现命令”执行验证。
